name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: babe051
          password:  dckr_pat_p5LsYi5jmFq4tvTb-2sQz3PWa_Q

      - name: Build Docker image
        run: |
          docker build -t babe051/mini_projet_django:1.1 .

      - name: Push Docker image
        run: |
          docker push babe051/mini_projet_django:1.1
